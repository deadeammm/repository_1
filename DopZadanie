"""
Написать программу на Python, которая генерирует координаты (строка, столбец) для элементов в "зигзагообразной" матрице
размером n x n начиная с правого верхнего угла.
"""

# Задаем переменную n равную 8
n = 8

# Создаем цикл для переменной k от 0 до 2*n-2 включительно
for k in range(2 * n - 1):
    # Внутренний цикл для переменной i от 0 до n-1 включительно
    for i in range(n):
        # Внутренний цикл для переменной j от 0 до n-1 включительно
        for j in range(n):
            # Проверяем условие i + j == k
            if i + j == k:
                # Проверяем условие k делится на 2 без остатка
                if k % 2 == 0:
                    # Если условие выполняется, то задаем переменные row и column
                    row, column = j, n - 1 - i
                else:
                    # Если условие не выполняется, то задаем переменные row и column
                    row, column = i, n - 1 - j
                # Выводим значения row и column в формате строки
                print(f"{row},{column}")
